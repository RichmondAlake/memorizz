# Procedural Persona Notes

This folder is kept for documentation continuity, but the active persona implementation lives under:

- `memorizz.long_term.semantic.persona`

Use:

```python
from memorizz.long_term.semantic.persona import Persona, RoleType
```

For full usage examples, see:

- `src/memorizz/long_term/semantic/persona/README.md`
