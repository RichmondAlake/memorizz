# Procedural Persona Notes

This folder is kept for documentation continuity, but the active persona implementation lives under:

- `memorizz.long_term_memory.semantic.persona`

Use:

```python
from memorizz.long_term_memory.semantic.persona import Persona, RoleType
```

For full usage examples, see:

- `src/memorizz/long_term_memory/semantic/persona/README.md`
